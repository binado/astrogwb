[runs.fixed]
sampled_params = ["H0"]

[runs.fixed.analysis]
detectors = ["S1", "R1", "C1"]

[runs.sampled]
sampled_params = ["H0", "local_merger_rate"]

[runs.sampled.analysis]
detectors = ["S1", "R1", "C1"]
