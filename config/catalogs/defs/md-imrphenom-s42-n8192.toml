# Half-size proposal for variable-catalog-size/n8192.
#
# `Predictive` allocates its per-draw keys with `jax.random.split`, which is
# prefix-stable, so this is an exact prefix of md-imrphenom-s42-n32768 -- the
# three sizes are nested draws, which is what makes the experiment a clean
# comparison rather than three unrelated ones.
num_samples = 8192
seed = 42
